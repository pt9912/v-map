// types/versions.d.ts  (TS kennt die Konstante & macht strenge Prüfung)
declare const CESIUM_VERSION:
  | `${number}.${number}.${number}`
  | `${number}.${number}`;
declare const OL_VERSION:
  | `${number}.${number}.${number}`
  | `${number}.${number}`;
