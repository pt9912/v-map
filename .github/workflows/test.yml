name: Test

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CI: true
      NODE_OPTIONS: --experimental-vm-modules
      PUPPETEER_CACHE_DIR: ~/.cache/puppeteer

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Get pnpm store path
        id: pnpm-store
        run: echo "path=$(pnpm store path)" >> "$GITHUB_OUTPUT"

      - name: Restore pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: pnpm-store-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            pnpm-store-${{ runner.os }}-

      - name: Install deps
        run: pnpm install --frozen-lockfile

      # ---------- System-Libs für Chrome/Chromium ----------
      - name: Install Chrome runtime dependencies
        run: |
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            libnss3 libnspr4 \
            libatk1.0-0 libatk-bridge2.0-0 libatspi2.0-0 \
            libcups2 libdrm2 libgbm1 \
            libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 \
            libx11-6 libx11-xcb1 libxcb1 libxext6 libxrandr2 libxrender1 libxshmfence1 \
            libglib2.0-0 \
            libcairo2 libpango-1.0-0 libpangocairo-1.0-0 \
            libasound2 \
            fonts-liberation \
            ca-certificates
          sudo rm -rf /var/lib/apt/lists/*

      # ---------- Browser vorbereiten ----------
      - name: Restore Puppeteer browser cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/puppeteer
          key: puppeteer-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            puppeteer-${{ runner.os }}-

      - name: Install chrome-headless-shell
        run: pnpm dlx @puppeteer/browsers install chrome-headless-shell@stable -P "$PUPPETEER_CACHE_DIR"

      - name: Resolve chrome-headless-shell path
        id: phs
        shell: bash
        run: |
          PHS_PATH="$(find "$PUPPETEER_CACHE_DIR" -type f -name 'chrome-headless-shell' | head -n1 || true)"
          if [[ -z "$PHS_PATH" ]]; then
            echo "No chrome-headless-shell found!" >&2
            exit 1
          fi
          echo "path=$PHS_PATH" >> "$GITHUB_OUTPUT"
          echo "PUPPETEER_EXECUTABLE_PATH=$PHS_PATH" >> "$GITHUB_ENV"

      # ---------- Tests ----------
      - name: Run unit/spec tests (Jest/jsdom)
        run: pnpm run test:spec

      - name: Run e2e tests (Puppeteer)
        env:
          PUPPETEER_EXECUTABLE_PATH: ${{ steps.phs.outputs.path }}
        run: pnpm run test:e2e
